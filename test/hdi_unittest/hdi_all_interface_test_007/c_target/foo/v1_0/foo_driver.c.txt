/*
 * Copyright (c) 2023 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

#include <hdf_base.h>
#include <hdf_device_desc.h>
#include <hdf_device_object.h>
#include <hdf_core_log.h>
#include <hdf_remote_service.h>
#include <osal_mem.h>
#include <stub_collector.h>
#include "v1_0/ifoo.h"

#define HDF_LOG_TAG    foo_driver

struct HdfFooHost {
    struct IDeviceIoService ioService;
    struct IFoo *service;
    struct HdfRemoteService **stubObject;
};

static int32_t FooDriverDispatch(struct HdfDeviceIoClient *client, int cmdId,
    struct HdfSBuf *data, struct HdfSBuf *reply)
{
    struct HdfFooHost *host = CONTAINER_OF(client->device->service, struct HdfFooHost, ioService);
    if (host->service == NULL || host->stubObject == NULL) {
        HDF_LOGE("%{public}s: invalid service obj", __func__);
        return HDF_ERR_INVALID_OBJECT;
    }

    struct HdfRemoteService *stubObj = *host->stubObject;
    if (stubObj == NULL || stubObj->dispatcher == NULL || stubObj->dispatcher->Dispatch == NULL) {
        return HDF_ERR_INVALID_OBJECT;
    }

    return stubObj->dispatcher->Dispatch((struct HdfRemoteService *)stubObj->target, cmdId, data, reply);
}

static int HdfFooDriverInit(struct HdfDeviceObject *deviceObject)
{
    HDF_LOGI("%{public}s: driver init start", __func__);
    return HDF_SUCCESS;
}

static int HdfFooDriverBind(struct HdfDeviceObject *deviceObject)
{
    HDF_LOGI("%{public}s: driver bind start", __func__);
    int32_t ret = HdfDeviceObjectSetInterfaceDesc(deviceObject, IFOO_INTERFACE_DESC);
    if (ret != HDF_SUCCESS) {
        HDF_LOGE("%{public}s: failed to set interface descriptor of device object", __func__);
        return ret;
    }

    struct HdfFooHost *host = (struct HdfFooHost *)OsalMemCalloc(sizeof(struct HdfFooHost));
    if (host == NULL) {
        HDF_LOGE("%{public}s: create HdfFooHost object failed!", __func__);
        return HDF_FAILURE;
    }

    struct IFoo *serviceImpl = IFooGet(true);
    if (serviceImpl == NULL) {
        HDF_LOGE("%{public}s: create serviceImpl failed!", __func__);
        OsalMemFree(host);
        return HDF_FAILURE;
    }

    struct HdfRemoteService **stubObj = StubCollectorGetOrNewObject(IFOO_INTERFACE_DESC, serviceImpl);
    if (stubObj == NULL) {
        HDF_LOGE("%{public}s: failed to get stub object", __func__);
        OsalMemFree(host);
        IFooRelease(serviceImpl, true);
        return HDF_FAILURE;
    }

    host->ioService.Dispatch = FooDriverDispatch;
    host->ioService.Open = NULL;
    host->ioService.Release = NULL;
    host->service = serviceImpl;
    host->stubObject = stubObj;
    deviceObject->service = &host->ioService;
    return HDF_SUCCESS;
}

static void HdfFooDriverRelease(struct HdfDeviceObject *deviceObject)
{
    HDF_LOGI("%{public}s: driver release start", __func__);
    if (deviceObject->service == NULL) {
        return;
    }

    struct HdfFooHost *host = CONTAINER_OF(deviceObject->service, struct HdfFooHost, ioService);
    if (host != NULL) {
        StubCollectorRemoveObject(IFOO_INTERFACE_DESC, host->service);
        IFooRelease(host->service, true);
        OsalMemFree(host);
    }
}

struct HdfDriverEntry g_fooDriverEntry = {
    .moduleVersion = 1,
    .moduleName = "",
    .Bind = HdfFooDriverBind,
    .Init = HdfFooDriverInit,
    .Release = HdfFooDriverRelease,
};

HDF_INIT(g_fooDriverEntry);
